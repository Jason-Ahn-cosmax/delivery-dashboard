version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install --omit=dev
run:
  runtime-version: 18.20.4
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
