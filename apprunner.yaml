version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --omit=dev
run:
  runtime-version: 22.21.1
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
