version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm install --omit=dev
run:
  runtime-version: 20
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
